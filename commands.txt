cd /Users/andrey/Documents/projects/MacPeripheralHub

# Собрать локальное macOS-приложение со всеми проверками:
scripts/package_app.sh

# Готовый app после сборки:
dist/MacPeripheralHub.app

# Файл контрольных сумм для проверки bundle:
dist/MacPeripheralHub.app.sha256

# Проверить app по checksum manifest:
cd dist
shasum -a 256 -c MacPeripheralHub.app.sha256
