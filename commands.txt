cd /Users/andrey/Documents/projects/MacPeripheralHub

# Собрать локальное macOS-приложение со всеми проверками:
scripts/package_app.sh

# Готовый app после сборки:
dist/MacPeripheralHub.app

# Архив для GitHub Release:
dist/MacPeripheralHub.app.zip

# Файл контрольной суммы для архива:
dist/MacPeripheralHub.app.zip.sha256

# Проверить release archive по checksum:
cd dist
shasum -a 256 -c MacPeripheralHub.app.zip.sha256
